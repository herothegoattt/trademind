{
  "name": "trademind",
  "version": "1.0.0",
  "engines": { "node": "22" },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node /home/site/wwwroot/.next/standalone/server.js"
  }
}
